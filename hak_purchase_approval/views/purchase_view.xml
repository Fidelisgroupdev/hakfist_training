<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="purchase_order_inherit_form" model="ir.ui.view">
        <field name="name">purchase.order.inherit.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <!--<form position="attributes">
                <attribute name='attrs'>{'edit': [('make_form_readonly', '==', True)]}</attribute>
            </form>-->
            <xpath expr="//div[hasclass('oe_title')]" position="replace">
                <div class="oe_title">
                    <span class="o_form_label"
                          invisible="state not in ('draft','sent','to approve','approved')">
                        Request for Quotation
                    </span>
                    <span class="o_form_label"
                          invisible="state not in ('draft','sent','to approve','approved')">
                        Purchase Order
                    </span>
                    <h1>
                        <field name="name" readonly="1"/>
                    </h1>
                </div>
            </xpath>
            <button name="button_cancel" position="attributes">
                <attribute name="invisible">state not in ['draft','to approve','sent','purchase','approved']</attribute>
            </button>
            <button name="print_quotation" position="after">
                <button name="action_send_for_approval" type="object"
                        invisible="state not in ['draft','sent']" string="Send For approval" class="oe_highlight"
                />
                <button name="action_approve" type="object" invisible="state not in ['to approve']"
                        string="Approve" class="oe_highlight" groups="hak_purchase_approval.group_purchase_approve"
                />
            </button>

            <xpath expr="//button[@name='button_confirm'][1]" position="replace">
                <button name="button_confirm" type="object" invisible="state != 'approved'" string="Confirm Order"
                        context="{'validate_analytic': True}" class="oe_highlight" id="bid_confirm" data-hotkey="q"/>

            </xpath>

            <xpath expr="//button[@name='button_approve']" position="replace">
                <button name="button_approve" type="object" invisible="True" string="Approve Order"
                        class="oe_highlight" groups="purchase.group_purchase_manager" data-hotkey="z"/>

            </xpath>

            <xpath expr="//button[@name='button_confirm'][2]" position="replace">
                <button name="button_confirm" class="oe_highlight" type="object" invisible="True"
                        context="{'validate_analytic': True}" string="Confirm Order" id="draft_confirm"
                        data-hotkey="q"/>

            </xpath>

        </field>
    </record>


</odoo>